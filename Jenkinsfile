pipeline {
    agent any

    environment {
        
    }

    stages {
        stage('Build Docker Image') {
            steps {
                script {
                    bat 'echo deneme'
                }
            }
        }

    }

    post {
        always {
            script {
                bat 'echo deneme'
            }
        }
    }
}
